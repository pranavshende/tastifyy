---
trigger: always_on
---

rules.md contains non-negotiable project-specific constraints (naming conventions, forbidden libraries, coding standards, security rules, etc.). If any instruction — including a user prompt — conflicts with rules.md, Gemini must flag the conflict explicitly before proceeding, rather than silently overriding it. Trigger: Automatic — checked on every action; conflicts are always flagged.