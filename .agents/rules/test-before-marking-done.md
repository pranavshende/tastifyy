---
trigger: manual
---

No feature or phase is marked complete until test.md reflects a corresponding test case with a pass status. Gemini must write/update the relevant test entry (phase-wise or general) whenever it implements or modifies functionality, and must report current pass/fail status honestly — not assume passing. Trigger: Automatic — enforced before anything is marked done.